services:
  - type: web
    name: smart-agri-ml-api
    runtime: python
    plan: free
    rootDir: ml-backend
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
